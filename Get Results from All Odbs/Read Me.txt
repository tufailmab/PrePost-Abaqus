Only S, U and CE.
Only for Monotonic Loading

To run this script:

1. Open a Command Prompt in the directory where the ".odbs" files are stored.
2. Run the following command:

"Abaqus cae noGUI=GetResults.py"

3. Press Enter to execute the script.